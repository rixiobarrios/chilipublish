// Chilipublish code challenge

let index = 4;

// Your function returns a number but you're not saving it
// index = changeIndex(index);
// index = changeIndex(index);
// changeIndex(index);
// changeIndex(index);

// if (index > 5) {
//     console.log('Index bigger than 5');
// } else {
//     console.log('Index less than or equal to 5');
// }

// function changeIndex(index) {
//     return index + 1;
// }

// output should be "Index bigger than 5"
